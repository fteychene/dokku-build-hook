#!/usr/bin/env bash
set -eo pipefail; [[ $DOKKU_TRACE ]] && set -x
source "$PLUGIN_CORE_AVAILABLE_PATH/common/functions"

APP="$1";
dokku_log_info1 "Checking for pre-build script"
if [ -f "hooks/pre-build" ]; then
    dokku_log_info2 "Detected pre-build script"
    hook/pre-build
fi